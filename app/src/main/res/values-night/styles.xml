<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/colorBackground</item>

        <item name="toolbarStyle">@style/Widget.App.Toolbar</item>

        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/Widget.ActionMode</item>
        <item name="actionModeCloseDrawable">@drawable/ic_close</item>
        <item name="actionModeCloseButtonStyle">@style/ActionModeCloseButtonStyle</item>
    </style>

    <style name="Widget.App.Toolbar" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Toolbar</item>
    </style>

    <style name="ThemeOverlay.App.Toolbar" parent="">
        <item name="colorPrimary">@color/colorBackground</item>
        <item name="colorOnPrimary">?colorOnBackground</item>
    </style>

    <style name="Widget.ActionMode" parent="Base.Widget.AppCompat.ActionMode">
        <item name="background">@color/colorBackgroundVariant</item>
    </style>

    <style name="ActionModeCloseButtonStyle" parent="Widget.AppCompat.ActionButton.CloseMode">
        <item name="android:tint">?attr/colorControlNormal</item>
    </style>
</resources>